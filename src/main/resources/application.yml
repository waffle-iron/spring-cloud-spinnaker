spinnaker:
  modules:
    - clouddriver
    - echo
    - front50
    - gate
    - igor
    - orca

security:
  user:
    name: user
    password: password

multipart:
  max-file-size: 100Mb
  max-request-size: 100Mb

logging:
  level:
    reactor:
      core: DEBUG
    org:
      cloudfoundry: DEBUG
      springframework:
        web: TRACE
        cloud:
          deployer: DEBUG
